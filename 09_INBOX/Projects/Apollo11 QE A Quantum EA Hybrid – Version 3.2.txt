//+------------------------------------------------------------------+ //| Apollo11 QE A| //| Quantum EA Hybrid – Version 3.2 | //| https://www.apollo11quantum.com | //+------------------------------------------------------------------+ #property copyright "Apollo11 Quantum EA" #property version "3.2" #property strict //--- indicador no gráfico #property indicator_chart_window #property indicator_buffers 4 #property indicator_plots 3 //--- Plot 1: Gravitational Zone com cor dinâmica #property indicator_label1 "GravZone" #property indicator_type1 DRAW_COLOR_HISTOGRAM #property indicator_color1 clrGreen,clrOrange,clrRed #property indicator_width1 5 //--- Plot 2: Thermal Energy #property indicator_label2 "ThermalEnergy" #property indicator_type2 DRAW_HISTOGRAM #property indicator_color2 clrDodgerBlue #property indicator_width2 2 //--- Plot 3: POC Levels #property indicator_label3 "POCs" #property indicator_type3 DRAW_ARROW #property indicator_color3 clrGold #property indicator_width3 1 #include <Trade\Trade.mqh> input bool EnableDebug = false; // ativa debug via Print() input int TradeStartHour = 8; // janela de trading (hora servidor) input int TradeEndHour = 18; input double InitialLots = 0.01; input double MaxLots = 1.0; input double RiskPercent = 1.0; input int StopLossPoints = 150; input bool UseATRStopLoss = true; input double ATRSLMultiplier = 2.0; input bool UseTrailingStop = true; input double TrailingATRMultiplier = 0.5; // trailing = ATR * este valor input double TrailingATRStepMult = 0.2; // step = ATR * este valor input bool UseBreakEven = true; input int BreakEvenPoints = 30; input int BreakEvenProfitPoints = 10; input int MagicNumber = 11041982; //--- Buffers double ExtGravZoneBuffer[]; double ExtThermalEnergyBuffer[]; double ExtPOCBuffer[]; double ExtColorBuffer1[]; //--- Trade CTrade g_trade; //--- Modelos struct GravModel { double thermal_energy; double channel_vector; double poc_level; void UpdateThermalEnergy(const double &v[],int p) { int sz=ArraySize(v); if(sz<1){ thermal_energy=0; return; } double sum=0; for(int i=0;i<p&&i<sz;i++) sum+=v[i]; thermal_energy = sum>0 ? v[0]/sum*100.0 : 0; } void MapChannelVector(const double &pr[],const double &v[],int p) { int sz=ArraySize(v); if(sz<1){ channel_vector=0; return; } double ps=0,vs=0; for(int i=0;i<p&&i<sz;i++){ ps+=pr[i]*v[i]; vs+=v[i]; } channel_vector = vs>0 ? ps/vs : 0; } void IdentifyPOCs(const double &pr[],const double &v[],int p) { int sz=ArraySize(v); if(sz<1){ poc_level=0; return; } double mv=0; int idx=0; for(int i=0;i<p&&i<sz;i++) if(v[i]>mv){ mv=v[i]; idx=i; } poc_level=pr[idx]; } } g_grav; struct InstRadar { double volume_pulse; double neural_confidence; void ScanInstitutionalPulse(const double &v[],int p) { int sz=ArraySize(v); if(sz<1){ volume_pulse=1; return; } double sum=0; for(int i=0;i<p&&i<sz;i++) sum+=v[i]; double avg = sum>0 ? sum/p : 1; volume_pulse = avg>0 ? v[0]/avg : 1; } void TuneCosmicFrequency(const double &v[],int p) { if(p<3){ neural_confidence=0; return; } int peaks=0; for(int i=1;i<p-1;i++) if(v[i]>v[i-1] && v[i]>v[i+1]) peaks++; double freq=(double)peaks/p; neural_confidence = MathMin(freq*100.0,100.0); } } g_inst; //--- Helpers para partial close struct TradeHelper { double tp1,tp2; // alvos double halfVolume; // metade do lote } g_helper; //+------------------------------------------------------------------+ //| OnInit | //+------------------------------------------------------------------+ int OnInit() { // bind buffers SetIndexBuffer(0,ExtGravZoneBuffer, INDICATOR_DATA); SetIndexBuffer(1,ExtThermalEnergyBuffer,INDICATOR_DATA); SetIndexBuffer(2,ExtPOCBuffer, INDICATOR_DATA); SetIndexBuffer(3,ExtColorBuffer1, INDICATOR_COLOR_INDEX); ArraySetAsSeries(ExtGravZoneBuffer,true); ArraySetAsSeries(ExtThermalEnergyBuffer,true); ArraySetAsSeries(ExtPOCBuffer,true); ArraySetAsSeries(ExtColorBuffer1,true); // config trade g_trade.SetExpertMagicNumber(MagicNumber); g_trade.SetDeviationInPoints(10); if(EnableDebug) Print("OnInit: iniciado."); return(INIT_SUCCEEDED); } //+------------------------------------------------------------------+ //| OnDeinit | //+------------------------------------------------------------------+ void OnDeinit(const int) { ObjectsDeleteAll(0); } //+------------------------------------------------------------------+ //| OnTick | //+------------------------------------------------------------------+ void OnTick() { // janela int hh = TimeHour(TimeCurrent()); if(hh<TradeStartHour || hh>=TradeEndHour) return; if(!IsTradeAllowed()) return; // copiar séries const int B=100; static double H[],L[],C[],V[]; static long VI[]; ArraySetAsSeries(H,true); ArraySetAsSeries(L,true); ArraySetAsSeries(C,true); ArraySetAsSeries(V,true); ArraySetAsSeries(VI,true); if(CopyHigh(_Symbol,PERIOD_CURRENT,0,B,H)!=B) return; if(CopyLow (_Symbol,PERIOD_CURRENT,0,B,L)!=B) return; if(CopyClose(_Symbol,PERIOD_CURRENT,0,B,C)!=B) return; if(CopyTickVolume(_Symbol,PERIOD_CURRENT,0,B,VI)!=B) return; ArrayResize(V,B); for(int i=0;i<B;i++) V[i]=(double)VI[i]; // atualizar indicadores internos g_grav.UpdateThermalEnergy(V,20); g_grav.MapChannelVector(C,V,20); g_grav.IdentifyPOCs(C,V,20); g_inst.ScanInstitutionalPulse(V,20); g_inst.TuneCosmicFrequency(V,20); // sinal int sig = CheckForSignal(); if(EnableDebug) Print("Sinal=",sig, " TE=",g_grav.thermal_energy, " VP=",g_inst.volume_pulse, " NC=",g_inst.neural_confidence); // gerenciar stops / BE / trailing ManagePositions(); // gerenciar partial close ManagePartial(); // abrir nova posição if(sig!=0) ExecuteSignal(sig); // atualizar gráfico UpdateIndicators(); } //+------------------------------------------------------------------+ //| Checa permissão de trading | //+------------------------------------------------------------------+ bool IsTradeAllowed() { if(!MQLInfoInteger(MQL_TRADE_ALLOWED) || !AccountInfoInteger(ACCOUNT_TRADE_EXPERT)) { if(EnableDebug) Print("Trading off."); return false; } long mode=SymbolInfoInteger(_Symbol,SYMBOL_TRADE_MODE); if(mode!=SYMBOL_TRADE_MODE_FULL) { if(EnableDebug) Print("Símbolo não negociável."); return false; } return true; } //+------------------------------------------------------------------+ //| Gera sinal BUY=+1, SELL=-1, NONE=0 | //+------------------------------------------------------------------+ int CheckForSignal() { // ajustar thresholds a gosto if(g_grav.thermal_energy > 25.0 && g_inst.volume_pulse > 1.5 && g_inst.neural_confidence> 30.0) return +1; if(g_grav.thermal_energy < 15.0 && g_inst.volume_pulse < 0.8 && g_inst.neural_confidence> 30.0) return -1; return 0; } //+------------------------------------------------------------------+ //| Calcula SL com ATR ou fixo | //+------------------------------------------------------------------+ double GetSLprice(bool isBuy) { double point = SymbolInfoDouble(_Symbol,SYMBOL_POINT); double sl_pts = (double)StopLossPoints; if(UseATRStopLoss) { int h=iATR(_Symbol,PERIOD_CURRENT,14); if(h!=INVALID_HANDLE) { double buf[]; if(CopyBuffer(h,0,0,1,buf)>0) sl_pts = buf[0]*ATRSLMultiplier/point; IndicatorRelease(h); } } double price = isBuy ? SymbolInfoDouble(_Symbol,SYMBOL_ASK) - sl_pts*point : SymbolInfoDouble(_Symbol,SYMBOL_BID) + sl_pts*point; return NormalizeDouble(price,_Digits); } //+------------------------------------------------------------------+ //| Calcula TP1 e TP2 dinâmicos via ATR+Fibo | //+------------------------------------------------------------------+ void CalculateDynamicTP(int sig,double entry, double &tp1,double &tp2) { double p = SymbolInfoDouble(_Symbol,SYMBOL_POINT); // ATR int h=iATR(_Symbol,PERIOD_CURRENT,14); double atr=0; if(h!=INVALID_HANDLE) { double b[]; ArraySetAsSeries(b,true); if(CopyBuffer(h,0,0,1,b)>0) atr=b[0]; IndicatorRelease(h); } // swing fibo double H[50],L[50]; ArraySetAsSeries(H,true); ArraySetAsSeries(L,true); CopyHigh(_Symbol,PERIOD_CURRENT,0,50,H); CopyLow (_Symbol,PERIOD_CURRENT,0,50,L); double sh=H[0], sl=L[0]; for(int i=1;i<50;i++){ sh=MathMax(sh,H[i]); sl=MathMin(sl,L[i]); } double range=sh-sl; double f236 = sh - range*0.236; double f382 = sh - range*0.382; // definir if(sig>0) { tp1 = entry + MathMin(atr*2, f236-entry); tp2 = entry + MathMax(atr*3, f382-entry); } else { tp1 = entry - MathMin(atr*2, entry-f236); tp2 = entry - MathMax(atr*3, entry-f382); } tp1 = NormalizeDouble(tp1,_Digits); tp2 = NormalizeDouble(tp2,_Digits); } //+------------------------------------------------------------------+ //| Executa sinal e salva dados de partial | //+------------------------------------------------------------------+ void ExecuteSignal(int sig) { if(PositionSelect(_Symbol)) return; double lots = CalculateLots(); if(lots<=0) return; bool isBuy = sig>0; double entry = isBuy ? SymbolInfoDouble(_Symbol,SYMBOL_ASK) : SymbolInfoDouble(_Symbol,SYMBOL_BID); double sl = GetSLprice(isBuy); CalculateDynamicTP(sig,entry, g_helper.tp1, g_helper.tp2); // abre sem TP bool ok = isBuy ? g_trade.Buy (lots, NULL, entry, sl, 0) : g_trade.Sell(lots, NULL, entry, sl, 0); if(!ok){ if(EnableDebug) Print("Erro ExecSignal: ", GetLastError()); return; } // partial g_helper.halfVolume = NormalizeDouble(lots/2,_Digits); if(EnableDebug) Print("Opened ", (isBuy?"BUY":"SELL"), " lot=",lots," SL=",sl, " TP1=",g_helper.tp1," TP2=",g_helper.tp2); } //+------------------------------------------------------------------+ //| Fecha metade em TP1 e resto em TP2 | //+------------------------------------------------------------------+ void ManagePartial() { if(!PositionSelect(_Symbol)) return; ulong ticket = PositionGetTicket(0); double vol = PositionGetDouble(POSITION_VOLUME); int type =(int)PositionGetInteger(POSITION_TYPE); double price = PositionGetDouble(POSITION_PRICE_CURRENT); // primeira metade if(g_helper.halfVolume>0 && vol>=g_helper.halfVolume) { if((type==POSITION_TYPE_BUY && price>=g_helper.tp1) || (type==POSITION_TYPE_SELL&& price<=g_helper.tp1)) { g_trade.PositionClosePartial(ticket, g_helper.halfVolume); if(EnableDebug) Print("Partial1 closed ",g_helper.halfVolume); g_helper.halfVolume=0; } } // segunda metade if(g_helper.halfVolume==0 && vol>0) { if((type==POSITION_TYPE_BUY && price>=g_helper.tp2) || (type==POSITION_TYPE_SELL&& price<=g_helper.tp2)) { g_trade.PositionClose(ticket); if(EnableDebug) Print("Partial2 closed rest"); } } } //+------------------------------------------------------------------+ //| Gerencia BE e Trailing ATR | //+------------------------------------------------------------------+ void ManagePositions() { int total=PositionsTotal(); for(int i=0;i<total;i++) { ulong ticket=PositionGetTicket(i); if(ticket==0) continue; if(!PositionSelectByTicket(ticket)) continue; if((int)PositionGetInteger(POSITION_MAGIC)!=MagicNumber) continue; double profit = PositionGetDouble(POSITION_PROFIT); double open = PositionGetDouble(POSITION_PRICE_OPEN); double current= PositionGetDouble(POSITION_PRICE_CURRENT); int type = (int)PositionGetInteger(POSITION_TYPE); // Break-Even if(UseBreakEven && profit>BreakEvenProfitPoints*_Point) { double new_sl = (type==POSITION_TYPE_BUY) ? open+BreakEvenPoints*_Point : open-BreakEvenPoints*_Point; g_trade.PositionModify(ticket,new_sl,PositionGetDouble(POSITION_TP)); } // Trailing ATR if(UseTrailingStop && profit>0) { // pega ATR int h=iATR(_Symbol,PERIOD_CURRENT,14); double atr=0; if(h!=INVALID_HANDLE) { double b[]; ArraySetAsSeries(b,true); if(CopyBuffer(h,0,0,1,b)>0) atr=b[0]; IndicatorRelease(h); } double dist = atr*TrailingATRMultiplier; double step = atr*TrailingATRStepMult; double desired = (type==POSITION_TYPE_BUY) ? current - dist : current + dist; double cur_sl = PositionGetDouble(POSITION_SL); bool move = (type==POSITION_TYPE_BUY) ? desired>cur_sl+step : desired<cur_sl-step; if(move) g_trade.PositionModify(ticket,desired,PositionGetDouble(POSITION_TP)); } } } //+------------------------------------------------------------------+ //| Calcula lote baseado em risco | //+------------------------------------------------------------------+ double CalculateLots() { double bal=AccountInfoDouble(ACCOUNT_BALANCE); double risk=bal*RiskPercent/100.0; double tickV=SymbolInfoDouble(_Symbol,SYMBOL_TRADE_TICK_VALUE); double tickS=SymbolInfoDouble(_Symbol,SYMBOL_TRADE_TICK_SIZE); double pt=SymbolInfoDouble(_Symbol,SYMBOL_POINT); double minL=SymbolInfoDouble(_Symbol,SYMBOL_VOLUME_MIN); double step=SymbolInfoDouble(_Symbol,SYMBOL_VOLUME_STEP); // SL em pontos double slp=StopLossPoints; if(UseATRStopLoss) { int h=iATR(_Symbol,PERIOD_CURRENT,14); if(h!=INVALID_HANDLE) { double b[]; if(CopyBuffer(h,0,0,1,b)>0) slp=b[0]/pt*ATRSLMultiplier; IndicatorRelease(h); } } if(slp<=0||tickV<=0||tickS<=0) return(InitialLots); double lots = risk/(slp*tickV*pt/tickS); lots = NormalizeDouble(lots,2); lots = MathMax(lots,minL); lots = MathMin(lots,MaxLots); int steps=(int)(lots/step); lots = step*steps; if(lots<minL) lots=minL; return NormalizeDouble(lots,2); } //+------------------------------------------------------------------+ //| Atualiza buffers e cores do indicador | //+------------------------------------------------------------------+ void UpdateIndicators() { int bars = Bars(_Symbol,PERIOD_CURRENT); int lim = MathMin(bars,ArraySize(ExtGravZoneBuffer)); for(int i=0;i<lim;i++) { ExtGravZoneBuffer[i] = g_grav.channel_vector; ExtThermalEnergyBuffer[i]= g_grav.thermal_energy; ExtPOCBuffer[i] = g_grav.poc_level; // cor: 0=green(<25),1=orange,2=red(>50) if(g_grav.thermal_energy>50) ExtColorBuffer1[i]=2; else if(g_grav.thermal_energy>25) ExtColorBuffer1[i]=1; else ExtColorBuffer1[i]=0; } ChartRedraw(); }